{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "allowJs": true,
    "checkJs": false,
    "noEmit": true,
    "isolatedModules": true,
    "resolveJsonModule": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,

    /* Strictest settings — apply in full to new .ts/.tsx files. Existing
       .js/.jsx files are included (allowJs) but not type-checked (checkJs:
       false), so this strictness is a floor for new TypeScript, not a
       retroactive requirement on the JS that already exists. */
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "exactOptionalPropertyTypes": true,
    "noImplicitReturns": true,
    "verbatimModuleSyntax": false
  },
  "include": ["src", "vite.config.js"],
  // supabase/functions/** are Deno Edge Functions (URL imports, `Deno.*`
  // globals) — a different runtime and module system entirely. "include"
  // above already omits them, but they're listed here too so widening
  // "include" later doesn't silently start type-checking Deno code with a
  // Node/browser tsconfig. Do not add them to "include".
  "exclude": ["node_modules", "dist", "supabase/functions"]
}
